apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.worker.name }}
spec:
  minUnavailable: {{ .Values.pdbServer.minUnavailable }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.worker.name }}
